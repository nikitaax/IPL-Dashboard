import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['scores.iplt20.com','documents.iplt20.com'], // Add the external domain here
  },
};

export default nextConfig;
